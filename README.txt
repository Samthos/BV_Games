To project requires the following libraries to be built

	1) SDL2
	2) SDL2_ttf
	3) pugixml

To build the project, you will mostly likely need to configure 
the Makefile.in to you systems specifications. 

	1) mkdir build
	2) cp Makefile.in build/Makefile
	3) Make
